# PostgreSQL connection string for Neon (do NOT commit real secrets)
DATABASE_URL=''

# Public base URL for your app (Render URL or custom domain)
BASE_URL=https://your-app.onrender.com

# Port to run local server
PORT=3000

# JWT secret for signing tokens
JWT_SECRET=''

# Cookie name for auth token (HTTP-only cookie)
JWT_COOKIE_NAME=''

# Node environment
NODE_ENV=development